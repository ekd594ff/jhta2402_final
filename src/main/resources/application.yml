spring:
  config:
    import: optional:file:.env[.properties]
  devtools:
    livereload:
      enabled: true
  application:
    name: home
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres_16.3:5432/cocolabhub
    username: cocolabhub
    password: cocolabhub
  jpa:
    hibernate:
      ddl-auto: create
      naming:
        # jpa camelCase
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    # script 파일이 hibernate 초기화 이후 동작하게 하기 위한 옵션
    defer-datasource-initialization: true
  mvc:
    # WebMvcConfig 에 있는 ** 정규식 패턴 사용하기 위함
    pathmatch:
      matching-strategy: ant_path_matcher

server:
  port: ${SPRING_BOOT_PORT:8080} # .env 파일 없으면 8080포트에서 실행

jwt:
  secret: faa358d3d58b5229ed9d67a58f5d6af4188c842f32765b6463ce69702a9f25ad9da60825898a53feee5f4dfa04e08029bf3d892f81ae1ec48d16e6f11431be8c
  token-validity-in-seconds: 1209600 # 14일
